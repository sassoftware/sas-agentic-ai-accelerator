# Python packages the compute context running Optimize-Prompt-DSPy.sas needs.
# Install them into the context's Python environment (see the "Enabling Prompt
# Optimization" page of the Administration Guide). dspy pulls in litellm,
# openai, pydantic and friends as its own dependencies.
#
# The dspy minimum matches the version the job's SCRLM adapter is validated
# against (the BaseLM response contract differs in older releases); the job
# checks it at startup and fails fast with a clear message when it is older.
dspy>=3.2.1
requests>=2.31
# Needed only for the MIPROv2 optimizer (its Bayesian trial search); the job
# checks for it at startup when MIPROv2 is chosen and fails fast without it.
optuna>=3.0

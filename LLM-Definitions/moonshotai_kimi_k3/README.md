# MoonshotAI: Kimi K3

MoonshotAI: Kimi K3 by OpenRouter, served through the OpenRouter API.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate moonshotai_kimi_k3` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | OpenRouter |
| Provider model | `moonshotai/kimi-k3` |
| Deployment type | API |
| License | Proprietary |
| Context length | 1048576 |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `temperature` | 1 | 0 - 2 | What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. |
| `top_p` | 1 | 0 - 1 | An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered. |
| `max_tokens` | 1000 | 1 - 1048576 (minus input token length) | Limits the amount of output tokens generated by the model. |
| `API_KEY` | OpenRouter | sk-**** | This is the OpenRouter key that is used to make the actual request. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The key arrives per call in the `API_KEY` option; the container needs no environment configuration beyond network access to the provider.

## Register and publish

```bash
mdb register moonshotai_kimi_k3
mdb publish moonshotai_kimi_k3 -d <your-destination>
```

Or do both in one step with `mdb ship moonshotai_kimi_k3`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

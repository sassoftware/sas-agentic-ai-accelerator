# us.anthropic.claude-haiku-4-5-20251001-v1:0

us.anthropic.claude-haiku-4-5-20251001-v1:0 by AWS Bedrock, served through the AWS Bedrock API.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate claude_haiku_4_5_bedrock` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | AWS Bedrock |
| Provider model | `us.anthropic.claude-haiku-4-5-20251001-v1:0` |
| Deployment type | API |
| License | Proprietary |
| Context length | unknown |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `temperature` | 1 | 0 - 2 | What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. |
| `top_p` | 1 | 0 - 1 | An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered. |
| `max_tokens` | 1000 | 1 - 200000 (minus input token length) | Limits the amount of output tokens generated by the model. |
| `API_KEY` | AWSBedrock | sk-**** | This is the AWSBedrock key that is used to make the actual request. |

## Register and publish

```bash
mdb register claude_haiku_4_5_bedrock
mdb publish claude_haiku_4_5_bedrock -d <your-destination>
```

Or do both in one step with `mdb ship claude_haiku_4_5_bedrock`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

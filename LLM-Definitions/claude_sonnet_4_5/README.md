# Claude Sonnet 4.5

Claude Sonnet 4.5 by Anthropic, served through the Anthropic API.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate claude_sonnet_4_5` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Anthropic |
| Provider model | `claude-sonnet-4-5-20250929` |
| Deployment type | API |
| License | Proprietary |
| Context length | 200000 |
| Knowledge cutoff | 2025-01-01 |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `temperature` | 1 | 0 - 2 | What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. |
| `top_p` | 1 | 0 - 1 | An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered. |
| `max_tokens` | 1000 | 1 - 64000 (minus input token length) | Limits the amount of output tokens generated by the model. |
| `thinking_budget` | 0 | 0 (off) or 1024 and above | Number of tokens an extended-thinking model may spend on internal reasoning before answering. 0 disables extended thinking. |
| `API_KEY` | Anthropic | sk-**** | This is the Anthropic key that is used to make the actual request. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The key arrives per call in the `API_KEY` option; the container needs no environment configuration beyond network access to the provider.

## Register and publish

```bash
mdb register claude_sonnet_4_5
mdb publish claude_sonnet_4_5 -d <your-destination>
```

Or do both in one step with `mdb ship claude_sonnet_4_5`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

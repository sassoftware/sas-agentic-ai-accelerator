# SmolLM 1.7B

SmolLM is a series of state-of-the-art small language models with 1.7B parameters

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate smollm_17b` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Apache-2 |
| Provider model | `HuggingFaceTB/SmolLM-1.7B-Instruct` |
| Deployment type | SCR |
| License | Open-Source |
| Context length | 2048 |
| Knowledge cutoff | 2024-06-01 |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `temperature` | 0.6 | 0 - 2 | What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. |
| `top_p` | 0.92 | 0 - 1 | An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered. |
| `max_tokens` | 100 | 1 - 2048 (minus input token length) | Limits the amount of output tokens generated by the model. |

## Register and publish

```bash
cd LLM-Definitions
python register-LLMs.py -l smollm_17b
python publish-LLMs.py -l smollm_17b -d <your-destination>
```

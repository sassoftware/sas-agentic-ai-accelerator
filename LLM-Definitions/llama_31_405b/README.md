# Llama 3.1 405B

Llama-3.1-405B-Instruct is a gigantic model that is hard to run.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate llama_31_405b` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Meta |
| Provider model | `llama_31_405b` |
| Deployment type | API |
| License | Open-Source |
| Context length | 128000 |
| Knowledge cutoff | 2023-12-01 |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `temperature` | 0.6 | 0 - 2 | What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. |
| `max_tokens` | 1024 | 1 - 128000 (minus input token length) | Limits the amount of output tokens generated by the model. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The key arrives per call in the `API_KEY` option; the container needs no environment configuration beyond network access to the provider.

## Register and publish

```bash
mdb register llama_31_405b
mdb publish llama_31_405b -d <your-destination>
```

Or do both in one step with `mdb ship llama_31_405b`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

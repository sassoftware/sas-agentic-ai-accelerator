# GPT-5 Mini

GPT-5 Mini by OpenAI, served through the OpenAI API.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate gpt_5_mini` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | OpenAI |
| Provider model | `gpt-5-mini` |
| Deployment type | API |
| License | Proprietary |
| Context length | 400000 |
| Knowledge cutoff | 2024-05-30 |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `reasoning_effort` | medium | minimal | low | medium | high | maximum | How much effort a reasoning model spends thinking before it answers. Higher levels improve quality on hard problems at the cost of latency and tokens. The normalized level is translated to what the specific provider supports (e.g. OpenAI reasoning_effort, Anthropic effort, Gemini thinking_level). |
| `max_completion_tokens` | 4000 | 1 - 128000 (includes reasoning tokens) | Limits the total tokens generated by a reasoning model, including internal reasoning tokens. Use this instead of max_tokens for reasoning models. |
| `API_KEY` | OpenAI | sk-**** | This is the OpenAI key that is used to make the actual request. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The key arrives per call in the `API_KEY` option; the container needs no environment configuration beyond network access to the provider.

## Register and publish

```bash
mdb register gpt_5_mini
mdb publish gpt_5_mini -d <your-destination>
```

Or do both in one step with `mdb ship gpt_5_mini`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

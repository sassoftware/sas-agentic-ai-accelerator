# Phi 3.5 Mini

Phi 3.5 Mini is an open-source model provided by Microsoft with a 128k token length.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate phi_35_mini` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Microsoft |
| Provider model | `phi_35_mini` |
| Deployment type | SCR |
| License | Open-Source |
| Context length | 128000 |
| Knowledge cutoff | 2023-10-01 |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `max_tokens` | 2048 | 1 - 128000 (minus input token length) | Limits the amount of output tokens generated by the model. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The key arrives per call in the `API_KEY` option; the container needs no environment configuration beyond network access to the provider.

## Register and publish

```bash
mdb register phi_35_mini
mdb publish phi_35_mini -d <your-destination>
```

Or do both in one step with `mdb ship phi_35_mini`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

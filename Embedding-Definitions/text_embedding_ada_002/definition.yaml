schema: 1
kind: embedding
model_id: text_embedding_ada_002
display_name: Text Embedding Ada 002
provider:
  adapter: openai
  model_version: text-embedding-ada-002
  endpoint: https://api.openai.com/v1/embeddings
  params: {}
  auth:
    mode: api_key
    key_name: OpenAI
runtime:
  template: emb_openai
  requirements_profile: api-wrapper
  timeout_s: 60
options:
  Embedding_Length:
    default: 1536
  Input_Token_Limit:
    default: 8192
tags:
  size_class: Embedding
  license_class: Proprietary
  provider_tag: OpenAI
  scr_sizing: small
  extra: []
metadata:
  description: text-embedding-ada-002 is OpenAIs improved, more performant version of OpenAIs ada embedding
    model. Embeddings are a numerical representation of text that can be used to measure the relatedness
    between two pieces of text. Embeddings are useful for search, clustering, recommendations, anomaly
    detection, and classification tasks.
  deployment_type: API
  pricing:
    cost_type: Tokens
    input_token_price: 1.0e-07
modeler: gerdaw
generation:
  overrides: []
  score_code_file: textEmbeddingAda002.py
  catalog_provenance: mdb import of hand-written folder

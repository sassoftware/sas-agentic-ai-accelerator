# Gemini Embedding 001

The Gemini API offers text embedding models to generate embeddings for words, phrases, sentences, and code.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate gemini_embedding_001` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Google |
| Provider model | `gemini-embedding-001` |
| Deployment type | API |
| License | Proprietary |
| Context length | unknown |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `Embedding_Length` | 3072 | 3072 | The length of the returned embedding vector. |
| `Input_Token_Limit` | 2048 | 2048 | The limit of input tokens for the embedding model. |
| `API_KEY` | Google | sk-**** | This is the Google key that is used to make the actual request. |

## Register and publish

```bash
cd LLM-Definitions
python register-LLMs.py -l gemini_embedding_001
python publish-LLMs.py -l gemini_embedding_001 -d <your-destination>
```

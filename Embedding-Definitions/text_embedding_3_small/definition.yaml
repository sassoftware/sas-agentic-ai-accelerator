schema: 1
kind: embedding
model_id: text_embedding_3_small
display_name: Text Embedding 3 Small
provider:
  adapter: openai
  model_version: text-embedding-3-small
  endpoint: https://api.openai.com/v1/embeddings
  params: {}
  auth:
    mode: api_key
    key_name: OpenAI
runtime:
  template: emb_openai
  requirements_profile: api-wrapper
  timeout_s: 60
options:
  Embedding_Length:
    default: 1536
  Input_Token_Limit:
    default: 8192
tags:
  size_class: Embedding
  license_class: Proprietary
  provider_tag: OpenAI
  scr_sizing: small
  extra: []
metadata:
  description: text-embedding-3-small is OpenAI's improved, more performant version of their ada embedding
    model. Embeddings are a numerical representation of text that can be used to measure the relatedness
    between two pieces of text. Embeddings are useful for search, clustering, recommendations, anomaly
    detection, and classification tasks.
  deployment_type: API
  pricing:
    cost_type: Tokens
    input_token_price: 2.0e-08
modeler: gerdaw
generation:
  overrides: []
  score_code_file: textEmbedding3Small.py
  catalog_provenance: mdb import of hand-written folder

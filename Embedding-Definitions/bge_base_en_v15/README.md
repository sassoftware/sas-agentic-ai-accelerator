# BGE Base EN v1.5

BAAI pre-trains the models using retromae and trains them on large-scale pairs data using contrastive learning.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate bge_base_en_v15` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | BAAI |
| Provider model | `BAAI/bge-base-en-v1.5` |
| Deployment type | SCR |
| License | Open-Source |
| Context length | unknown |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `Embedding_Length` | 768 | 768 | The length of the returned embedding vector. |
| `Input_Token_Limit` | 512 | 512 | The limit of input tokens for the embedding model. |

## Deployment

What the SCR container needs from its environment (the caller-side options are in the table above):

- The weights are baked into the image at build time; the container needs no provider connection and no key.

## Register and publish

```bash
mdb register bge_base_en_v15
mdb publish bge_base_en_v15 -d <your-destination>
```

Or do both in one step with `mdb ship bge_base_en_v15`. The publishing destination can also come from `SAS_PUBLISH_DESTINATION` in your `.env`.

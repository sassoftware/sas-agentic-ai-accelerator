schema: 1
kind: embedding
model_id: bge_base_en_v15
display_name: BGE Base EN v1.5
provider:
  adapter: hf-selfhosted
  model_version: BAAI/bge-base-en-v1.5
  params:
    hf:
      repo: BAAI/bge-base-en-v1.5
      gated: false
  auth:
    mode: none
runtime:
  template: emb_sentence_transformers
  requirements_profile: hf-sentence-transformers
  timeout_s: 60
options:
  Embedding_Length:
    default: 768
  Input_Token_Limit:
    default: 512
tags:
  size_class: Embedding
  license_class: Open-Source
  provider_tag: BAAI
  scr_sizing: medium
  extra:
  - MIT
metadata:
  description: BAAI pre-trains the models using retromae and trains them on large-scale pairs data using
    contrastive learning.
  size: 109000000
  deployment_type: SCR
  pricing:
    cost_type: Seconds
    second_cost: 3.9178e-05
modeler: gerdaw
generation:
  overrides: []
  score_code_file: bgeBaseENv15.py
  catalog_provenance: mdb import of hand-written folder

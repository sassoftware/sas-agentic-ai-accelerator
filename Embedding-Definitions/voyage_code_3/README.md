# Voyage Code 3

Voyage Code 3 by Voyage, served through the Voyage AI API.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate voyage_code_3` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | Voyage |
| Provider model | `voyage-code-3` |
| Deployment type | API |
| License | Proprietary |
| Context length | 32000 |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `input_type` | document | document | query | Whether the text is embedded as a document (for storage/indexing) or as a query (for retrieval). Using the matching type on both sides improves retrieval quality. |
| `Embedding_Length` | 1024 | 1024 | The length of the returned embedding vector. |
| `Input_Token_Limit` | 32000 | 32000 | The limit of input tokens for the embedding model. |
| `API_KEY` | Voyage | sk-**** | This is the Voyage key that is used to make the actual request. |

## Register and publish

```bash
cd LLM-Definitions
python register-LLMs.py -l voyage_code_3
python publish-LLMs.py -l voyage_code_3 -d <your-destination>
```

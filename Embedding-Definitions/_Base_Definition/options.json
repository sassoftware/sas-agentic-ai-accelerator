{
    "API_KEY": {
        "default": "Provider",
        "range": "sk-****",
        "description": "This is the Provider key that is used to make the actual request."
    },
    "Embedding_Length": {
        "default": 3072,
        "range": "3072",
        "description": "The length of the returned embedding vector."
    },
    "Input_Token_Limit": {
        "default": 2048,
        "range": "2048",
        "description": "The limit of input tokens for the embedding model."
    }
}
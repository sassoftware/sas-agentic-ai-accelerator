# BGE Large EN v1.5

BAAI pre-trains the models using retromae and trains them on large-scale pairs data using contrastive learning.

This definition was generated by the [Model Definition Builder](../../Model-Definition-Builder/README.md) from `definition.yaml` — edit that file and run `mdb generate bge_large_en_v15` instead of editing the generated files by hand.

## Model facts

| Attribute | Value |
| --------- | ----- |
| Provider | BAAI |
| Provider model | `BAAI/bge-large-en-v1.5` |
| Deployment type | SCR |
| License | Open-Source |
| Context length | unknown |
| Knowledge cutoff | unknown |

## Configuration options

The following options can be supplied at scoring time through the `options` input (see the [LLM-Definitions README](../README.md) for the format):

| Option | Default | Range | Description |
| ------ | ------- | ----- | ----------- |
| `Embedding_Length` | 384 | 384 | The length of the returned embedding vector. |
| `Input_Token_Limit` | 512 | 512 | The limit of input tokens for the embedding model. |

## Register and publish

```bash
cd LLM-Definitions
python register-LLMs.py -l bge_large_en_v15
python publish-LLMs.py -l bge_large_en_v15 -d <your-destination>
```

llm-prompt-builder.json
rag-builder.json
portal-page-layout.json
sas-viya
sas-viya.exe
sas-viya-cli-commands.txt
.venv/

# Local credentials for the setup scripts (never commit) — see .env.example
.env
.env.local

# Python bytecode
__pycache__/
*.pyc
# Python build artifacts
*.egg-info/

# SAS Viya deployment assets — site-specific, order-numbered and large; never commit
SASViyaV4_*_deploymentAssets_*.tgz

# Retired model definitions archived out of the active set by `mdb retire`
_archive/

# Rendered SCR deployment manifests (`mdb deploy`) — site-specific: they carry
# your container registry, ingress host and Azure resource. Regenerate, never commit.
deploy-yaml/

# Design notes: thinking-in-progress for features not yet built, kept beside
# the code while they are being worked out. They go stale the moment the
# feature ships, and a stale design doc is read as documentation.
LLM-Prompt-Builder/docs/
